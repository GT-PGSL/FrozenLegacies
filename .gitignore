# ── Large data (never upload to GitHub) ──────────────────────
Data/ascope/raw/          # 12 GB raw TIFF scans
Data/zscope/              # Z-scope raw data
Data/BEDMAP/              # BEDMAP1 reference grids
Zipfiles/                 # Compressed archives

# ── Science-critical files (hold until post-LYRA) ───────────
Radiometric_Calibrator/
Attenuation/

# ── Documentation (kept locally, not on GitHub) ─────────────
/docs/
/old_docs/
CLAUDE.md

# ── Retired tools ───────────────────────────────────────────
CalpipPicker/

# ── Generated outputs (reproducible from code + data) ───────
**/output/
**/OUTPUT/
**/backup/
**/backup_copies/

# ── Trained ML models (too large for GitHub) ────────────────
tools/LYRA/models/*.pkl

# ── Python build artifacts ──────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/

# ── macOS system files ──────────────────────────────────────
.DS_Store
.AppleDouble
.LSOverride

# ── Claude Code private config ──────────────────────────────
.claude/

# ── LaTeX auxiliary files ───────────────────────────────────
*.aux
*.log
*.toc
*.out
*.synctex.gz

# ── IDE / editor ────────────────────────────────────────────
.vscode/
*.code-workspace
